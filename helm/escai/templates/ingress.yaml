{{- if .Values.escai.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "escai.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "escai.labels" . | nindent 4 }}
  {{- with .Values.escai.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.escai.ingress.className }}
  ingressClassName: {{ .Values.escai.ingress.className }}
  {{- end }}
  {{- if .Values.escai.ingress.tls }}
  tls:
    {{- range .Values.escai.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.escai.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "escai.fullname" $ }}
                port:
                  number: {{ $.Values.escai.service.port }}
          {{- end }}
    {{- end }}
{{- end }}